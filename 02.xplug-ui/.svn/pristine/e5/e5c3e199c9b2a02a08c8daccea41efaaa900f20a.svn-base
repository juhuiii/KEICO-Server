import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ModKwhComponent } from './mod-kwh.component';

describe('ModKwhComponent', () => {
  let component: ModKwhComponent;
  let fixture: ComponentFixture<ModKwhComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ModKwhComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ModKwhComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
