import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ModalCtrlGroupComponent } from './modal-ctrl-group.component';

describe('ModalCtrlGroupComponent', () => {
  let component: ModalCtrlGroupComponent;
  let fixture: ComponentFixture<ModalCtrlGroupComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ModalCtrlGroupComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ModalCtrlGroupComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
