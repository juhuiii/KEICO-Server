import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FindPlugComponent } from './find-plug.component';

describe('FindPlugComponent', () => {
  let component: FindPlugComponent;
  let fixture: ComponentFixture<FindPlugComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ FindPlugComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(FindPlugComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
