import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { MeasRptHourComponent } from './meas-rpt-hour.component';

describe('MeasRptHourComponent', () => {
  let component: MeasRptHourComponent;
  let fixture: ComponentFixture<MeasRptHourComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ MeasRptHourComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(MeasRptHourComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
